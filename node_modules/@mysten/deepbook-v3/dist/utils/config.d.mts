import { BalanceManager, Coin, MarginManager, MarginPool, Pool } from "../types/index.mjs";
import { CoinMap, DeepbookPackageIds, MarginPoolMap, PoolMap } from "./constants.mjs";
import { BalanceManagerContract } from "../transactions/balanceManager.mjs";
import { SuiClientTypes } from "@mysten/sui/client";

//#region src/utils/config.d.ts
declare const FLOAT_SCALAR = 1000000000;
declare const DEEP_SCALAR = 1000000;
declare const MAX_TIMESTAMP = 1844674407370955161n;
declare const PRICE_INFO_OBJECT_MAX_AGE_MS = 30000;
declare const GAS_BUDGET = 250000000;
declare const POOL_CREATION_FEE_DEEP = 500000000;
declare class DeepBookConfig {
  #private;
  network: SuiClientTypes.Network;
  balanceManagers: {
    [key: string]: BalanceManager;
  };
  marginManagers: {
    [key: string]: MarginManager;
  };
  address: string;
  pyth: {
    pythStateId: string;
    wormholeStateId: string;
  };
  DEEPBOOK_PACKAGE_ID: string;
  REGISTRY_ID: string;
  DEEP_TREASURY_ID: string;
  MARGIN_PACKAGE_ID: string;
  MARGIN_V1: string;
  MARGIN_REGISTRY_ID: string;
  LIQUIDATION_PACKAGE_ID: string;
  adminCap?: string;
  marginAdminCap?: string;
  marginMaintainerCap?: string;
  balanceManager: BalanceManagerContract;
  constructor({
    network,
    address,
    adminCap,
    marginAdminCap,
    marginMaintainerCap,
    balanceManagers,
    marginManagers,
    coins,
    pools,
    marginPools,
    packageIds,
    pyth
  }: {
    network: SuiClientTypes.Network;
    address: string;
    adminCap?: string;
    marginAdminCap?: string;
    marginMaintainerCap?: string;
    balanceManagers?: {
      [key: string]: BalanceManager;
    };
    marginManagers?: {
      [key: string]: MarginManager;
    };
    coins?: CoinMap;
    pools?: PoolMap;
    marginPools?: MarginPoolMap;
    packageIds?: DeepbookPackageIds;
    pyth?: {
      pythStateId: string;
      wormholeStateId: string;
    };
  });
  requirePyth(): void;
  getCoin(key: string): Coin;
  getPool(key: string): Pool;
  getMarginPool(key: string): MarginPool;
  /**
   * @description Get the balance manager by key
   * @param managerKey Key of the balance manager
   * @returns The BalanceManager object
   */
  getBalanceManager(managerKey: string): BalanceManager;
  /**
   * @description Get the margin manager by key
   * @param managerKey Key of the margin manager
   * @returns The MarginManager object
   */
  getMarginManager(managerKey: string): MarginManager;
}
//#endregion
export { DEEP_SCALAR, DeepBookConfig, FLOAT_SCALAR, GAS_BUDGET, MAX_TIMESTAMP, POOL_CREATION_FEE_DEEP, PRICE_INFO_OBJECT_MAX_AGE_MS };
//# sourceMappingURL=config.d.mts.map